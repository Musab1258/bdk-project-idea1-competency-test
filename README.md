# Summer of Bitcoin Competency Test

This repository contains my competency test submission for the Summer of Bitcoin's BDK project Idea 1 (Improve CI persistence and MSRV testing, automate issue triage). 

## Requirements Completed
- [x] Create a repo in a personal GitHub account
- [x] Create and commit a simple Rust hello-world app
- [x] Properly sign commits using GPG
-[x] Add a GitHub Actions job to build and check formatting (`cargo build` and `cargo fmt`)
- [x] Add a feature branch to change the app
- [x] Create a Pull Request from the feature branch

## Running Locally

To run this project locally, ensure you have [Rust](https://rustup.rs/) installed, then run:

```bash
cargo run